show welcome message
pick a random int from 1-100
prompt user to guess & show user number of guesses & allow option to quit
if (guess == random number) {
    win and go back to line 2
}
if (guess < random number) {
    tell the user their guess was too low and go back to line 3;
    +1 to the number of guesses
}
if (guess > random number) {
    tell the user their guess was too high and go back to line 3;
    +1 to the number of guesses
}